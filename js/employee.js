(function ($) {


    Drupal.behaviors.employee = {
      attach: function (context, settings) {

        // $( "button#edit-submit").on("keyup",function() {
        //    alert();
        //   });

      }
    };
  }(jQuery));